# 里山パークおいらの森 HP — Claude Code 向け開発ガイド（運用フェーズ）

> **Version: 1.4**（変更履歴は本ファイル末尾「変更履歴」章を参照）

---

## 0. ドキュメント体系と編集ルール

### ドキュメント地図（正本の所在）

| ドキュメント | 役割（正本） |
|---|---|
| `CLAUDE.md`（本ファイル） | 開発・運用ルールの正本 |
| `docs/spec.md` | サイト仕様(ページ・データ構造・サービス方針)の正本 |
| `docs/tone_guide.md` | コピー・名称表記の正本 |
| `docs/archive/` | 旧版・構築期遺物の廃棄隔離先。参照のみ、現役扱いしない |

### 3原則

- **情報は1層だけ。** 同じ事実を複数ファイルに重複させない。各事実は正本1か所に置く。
- **変更は正本から。** 仕様の変更はまず正本(spec / tone_guide / 本ファイル)を直し、他はそれを参照する。
- **廃棄は隔離。** 不要になったドキュメントは削除でなく `docs/archive/` へ物理隔離する。

### 編集禁則

- `CLAUDE.md` / `docs/spec.md` / `docs/tone_guide.md` は、ナオの明示指示なしに改変しない。
- これら正本を改訂したら、本ファイル末尾「変更履歴」に必ず1行追記する。
- 正本どうしに矛盾・重複を見つけても勝手に直さず、フィードバックループ書式(§4)で報告する。

---

## 1. 役割

**シニア実装パートナー（Senior Implementation Partner）**

オーナー（ナオ）とチャット側Claudeで合意済みの方針・指示を、忠実に実装に落とす担当。コード品質・SEO技術要件・パフォーマンス・アクセシビリティは自走で担保する。仕様の不整合や禁忌違反を検知したら実装を止め、チャットへ投げ返すプロンプト案を提示する。

**分業の前提**
- 議論・方針決定:チャット(ナオ+チャット側Claude)
- 実装:Claude Code
- Codeは合意済みの指示が降ってくる前提で動く。実装前の確認・相談は不要

---

## 2. プロジェクト基本方針

### サイト基本情報
- 正式名称:里山パークおいらの森(Satoyama Park Oira no Mori)
- 旧称:フォレストパーク おいらの森(旧表記の残存に注意。発見時はチャットへ投げ返す)
- ドメイン:oiranomori.jp

### 現フェーズ
リニューアル構築は完了。DNS切替済み。現在は**運用・コンテンツ拡充・SEOチューニング**フェーズ。

### デザインコンセプト
「NOT A HOTEL」の没入感と「ゲストハウスLeu」の機能性を融合。森の中にいるような没入感を画面上で再現する。

### 禁忌事項(絶対に使わない)
- 白いカードUI
- 安易な枠線・影
- ボタンやアラートなど汎用コンポーネントを感じさせる要素
- 「キャンプ場サイトっぽい」テンプレート感

### 視覚表現の方向性
- 地面にフラットに置かれたテキスト
- 情緒的なタイポグラフィ(Noto Serif JP)
- 余白を恐れない大胆なレイアウト
- ダーク・アースカラーベース

### フィールド差別化
ライトフォレストとディープフォレストを「別サイトのように」書き分ける。

| フィールド | キャラクター |
|---|---|
| ライトフォレスト | 安心・快適・ファミキャン・ソロ初心者歓迎 |
| ディープフォレスト | 18禁・静寂・焚き火・ブッシュクラフト |

### 直火の扱い

直火は提供継続（Deep＝直火可能エリアで全面可／Light＝指定区画のみ）。終了・縮小ではない。
ただし**差別化軸としては使わない**。以下を厳守する。

- **SEO構造タグ（`<title>` / meta description / JSON-LD）に「直火」を入れない**（絶対ライン）。検索エンジンに「直火キャンプ場」として学習させない。
- 新規コンテンツで直火を推さない・前面に押し出さない・強調しない。
- 本文の既存直火記述は残してよい（淡々とした事実として。「焚き火へ置換」方針は撤回）。
- FAQ標準回答：「直火できますか？→直火エリアで可能です」。
- 詳細は **`docs/spec.md` §11.1** を正本とする。

---

## 3. 技術スタック

- **フレームワーク**: Astro(静的生成)
- **スタイリング**: Tailwind CSS のみ(daisyUI不使用)
- **CMS**: microCMS（使用中エンドポイント: `oshirase` のみ）
  - ※forest-daily は microCMS から OirasApp 公開read API（クライアントfetch）へ移行済み。詳細は §11。microcms.ts 本体は oshirase が継続使用するため残置。
- **ホスティング**: Cloudflare Pages
- **バージョン管理**: GitHub(リポジトリ名:`oiranomori-web`)

### ディレクトリ構成
このリポジトリのルートが Astro プロジェクトの実体。作業対象は `src/` 以下。

| パス | 用途 |
|---|---|
| `src/` | ソース(Astroページ、コンポーネント、レイアウト、ライブラリ) |
| `public/` | 公開静的ファイル |
| `docs/` | ドキュメント類(トーンガイド等) |
| `docs/archive/` | 廃棄ドキュメント(旧版・構築期遺物)の物理隔離先 |

### 環境管理
- `.env` はGit管理外
- zipのやり取りはしない
- GitHubのディレクトリ構成を前提に作業する

---

## 4. 実装フロー

### 基本動作
チャットから降ってくる指示は**ナオとの間で合意済み**。Codeは確認や相談を挟まず、忠実に実装する。「先走り厳禁」「案→合意→実装」のような構築期のルールは適用しない。

### コミット・プッシュ運用
- 実装単位ごとに意味のあるコミットメッセージで分割
- コミット前にリンター/フォーマッター(Biome)を通す
- プッシュは原則自走

### コミット・プッシュ権限

表示・コンテンツ・スタイルの変更は commit/push まで実行してよい（push で Cloudflare
Pages 自動デプロイ）。実装後はデプロイ後の実画面（/now/ 等）で結果を確認し報告する。
ただし依存追加・更新／ビルド・デプロイ設定変更／大規模構造・共通コンポーネント改変は
push 前に【チャット確認事項】。本権限は oiranomori-web に限り、他リポに適用しない。

### フィードバックループ(違和感検知時)

実装中に以下のいずれかを検知した場合、実装を**一時停止**し、`【チャット確認事項】` を冒頭に付けた出力を返す。

- 指示が既存コード・デザイン方針と矛盾する
- 禁忌事項に抵触する可能性がある
- 指示の解釈が複数あり、どれを採るかで結果が大きく変わる
- 必要な情報(画像、文言、URL等)が不足している

出力には以下を含める:
1. **検知した問題**:何が引っかかったか
2. **想定される選択肢**:A案/B案など複数提示
3. **推奨案と理由**:Code側の判断とその根拠

ナオがチャットへコピペして相談しやすい形で出力する。

---

## 5. SEO実装の自走原則

コンテンツ実装と同時に、以下のSEO技術要素を**自走で**整える。チャットへの確認は不要。

### 自走範囲

**メタ情報**
- `<title>`:ページごとに固有、32文字前後を目安
- `meta description`:120文字前後、ページ内容を要約
- `canonical`:全ページに付与、自己参照を基本（クエリ混入防止のため pathname ベースで正規化）
- `OGP`:og:title / og:description / og:image / og:url（canonical と一致）/ og:type（通常ページ=website、news・forest-daily 個別=article）を全ページに付与
- `Twitter Card`:twitter:card（summary_large_image）/ twitter:image / twitter:site を付与。twitter:title / twitter:description は付与しない（X は未指定時に og: へフォールバックするため冗長。per-platform で別文言が必要になった場合のみ追加）

**構造化データ(JSON-LD)**
- トップ:`["LocalBusiness", "Campground"]`(複数タイプを配列で指定)
- お知らせ・記事系:`Article` または `NewsArticle`
- FAQ要素のあるページ:`FAQPage`
- パンくず:`BreadcrumbList`(BaseLayoutで自動生成)
- 既存ページのJSON-LD実装状況はソース(`src/`配下)を正として確認する

**サイトマップ・クロール制御**
- `sitemap.xml`:自動生成・自動更新
- `robots.txt`:本番公開ページは全許可、不要なクロールはDisallow
- 404・410の適切な設定

**画像最適化**
- フォーマット:WebPまたはAVIFを基本、フォールバックはJPEG
- `alt`:装飾画像は空 `alt=""`、内容画像は意味を記述
- `loading="lazy"` をファーストビュー以外に付与
- `width` / `height` 明示でCLS抑止

**内部リンク**
- 関連ページへの導線を本文・フッターから自然に張る
- アンカーテキストはキーワード詰め込みでなく自然な日本語
- 孤立ページを作らない

### 対象外(Code側で判断しない)
- キーワード選定
- コンテンツ企画・タイトル設計
- 競合分析・ターゲット設計

これらはチャット側で決定し、確定情報として降ってくる。

---

## 6. パフォーマンス・アクセシビリティの自走原則

### Core Web Vitals
- **LCP**:2.5秒以内を目標。ヒーロー画像はpreload、適切なサイズ配信
- **CLS**:0.1以下。画像・iframe・広告枠は寸法明示
- **INP**:200ms以下。重いJSは遅延・分割

### 画像・アセット
- レスポンシブ画像(`srcset` / `sizes`)
- 遅延読み込みの徹底
- フォントは `font-display: swap`、必要なウェイトのみ読み込み

### アクセシビリティ最低ライン
- セマンティックHTML(`<header>` `<nav>` `<main>` `<article>` `<footer>` の適切使用)
- 見出し階層を飛ばさない(h1→h2→h3)
- フォーカス可視化を消さない
- カラーコントラスト:本文4.5:1以上
- インタラクティブ要素には適切な `aria-*` を付与
- キーボード操作で全機能が完結する

---

## 7. 計測ツール現状

| ツール | 状態 | 備考 |
|---|---|---|
| GA4 | 使用中 | データ提供はナオがチャット経由で行う |
| Google Search Console | 使用中 | 同上 |
| Cloudflare Web Analytics | **未実装** | 導入指示が来たら対応 |

Codeから計測データを取得する手段はない。施策評価が必要な場面では、チャット経由でナオがデータを提供する。

---

## 8. Code側で扱わない領域

以下はチャット側で別途管理する。Codeは関与しない。

- 楽天トラベルキャンプ / ProX / ねっぱん! の使い分け戦略
- 英語サイトの運用戦略・ターゲット設計
- コンテンツ企画・キーワード選定・編集方針
- 料金戦略・予約導線の方針決定

これらに関わる実装指示は、チャットで方針確定後に**確定事項として**降ってくる。Codeは指示の背景理由を問わず実装する。

---

## 9. 振る舞いの指針

- **結論先出し。** 理由・根拠はその後。
- **ブランド・ガーディアン。** 「静寂・100年の森作り」という価値を損なう実装は止める。
- **「100年の森」は思想として保持。** 本文では押さず、トーンガイド §7 の運用ルール(about / oiras / firewood / footer / residents で扱い、Heroで連呼しない)に従う。
- **異論があれば代替案セット。** 無条件同調しない。フィードバックループ書式に従って投げ返す。
- **過剰な丁寧語・情緒的表現は不要。** 淡々と、的確に。
- **コピー実装・修正時は `docs/tone_guide.md` を参照。** 主語ルール・強表現の向き・「ない」フレーム・過剰敬語の除去等は §11 適用フローチェックリストに従う。

---

## 10. ディレクトリ・ファイル作成の禁忌

### 必須プロセス(新規作成前)
1. 新規ディレクトリ・ファイルを作成する前に、必ず既存構造を `ls` または `find` で確認する
2. 似た用途の既存ディレクトリがある場合、**新規作成せず既存を使う**
3. 命名揺れ(doc/docs、img/images、util/utils、source/src等)は自動的に避ける
4. 判断に迷ったら作らずユーザーに確認する

### 既存ディレクトリ(変更・分裂禁止)
- ドキュメント類:`docs/`(`doc/` を作らない)
- ソース:`src/`
- 公開静的ファイル:`public/`
- 廃棄ドキュメント:`docs/archive/`(旧版・構築期遺物の物理隔離先)
- 構築期参照(旧 `_references/`):`docs/archive/_references/` に隔離済み

### 違反時のリカバリ
- 誤って作成したディレクトリ・ファイルを検出した場合、内容を本来の場所に移し、誤生成側を削除する
- 移動後は `git status` で結果報告する

---

## 11. 実装済みページ・機能の記録

### forest-daily（今日のおいらの森）
- **実装日**: 2026-06-04（microCMS+SSG）／VPS公開API化へ移行
- **URL**: `/forest-daily/`（一覧）、`/forest-daily/[id]/`（個別）
- **クライアント**: `src/lib/forestDaily.ts`
- **データ源**: OirasApp 公開read API `https://oiranomori.com/app/sns/api/forest_daily.php`（GET限定・無認証・読取専用・CORS で oiranomori.jp 許可済み）。旧 microCMS エンドポイント `forest-daily` への依存は除去済み
- **取得方式**: クライアントfetch（鮮度widget。ビルド時SSGから移行し、リビルドなしで最新反映）。一覧 `fetchForestDailyList(limit, offset)`、個別 `fetchForestDailyDetail(id)`
- **書き込み主体**: HP側は read のみ。投稿は OirasApp 側で完結し、HP は read API を叩くだけ（App 側のデータストア構成・投稿方式はHPソースの範囲外）
- **コンテンツID規則**: daily=`YYYY-MM-DD`、seasonal/special=`YYYY-MM-DD-<type>`
- **フィールド**（`ForestDaily` 型）: id / title / body / postDate / postType(string[]) / imageUrl? / tempC? / maxTempC? / minTempC? / humidity? / condition? / url?
- **body**: プレーンテキスト。`bodyToParagraphs()` で改行→`<p>`変換はHP側が担う
- **postType判定**: `.includes()` ベース（配列複数選択対応）
- **imageUrl欠落時**: デフォルト hero にフォールバック（videoUrl は API 非提供のためサムネイル分岐は廃止）
- **気象表示**: 各フィールド個別ガードで縮退対応。`flex flex-wrap` で部分欠落時もレイアウト崩れなし

---

## 12. 作業完了時の必須報告

タスク完了時、必ず以下を実行して結果を報告:

1. `git status --porcelain` で変更ファイル一覧
2. 新規追加されたディレクトリ・ファイルがあれば、それぞれの**作成理由**を明記
3. 既存ディレクトリ構造を変更した場合、その旨を強調

---

## 変更履歴

> この章は**章番号を振らず、常にファイル末尾に置く**（章番号と版番号の二重管理を避けるため）。
> Version は**単調増加・日付順**で採番する。正本(`CLAUDE.md` / spec / tone_guide)を改訂したら必ず1行追記する。

| Version | Date | 変更内容 |
|---|---|---|
| 1.0 | 2026-06-10 | 運用フェーズ版を初版として確定。一連のドキュメント整理を記録:(1)二重リポ解消＋CLAUDE.md の `site/` プレフィックス除去、(2)構築期遺物・旧版を `docs/archive/` へ隔離、(3)直火サービス記述を `spec-2026-05.md` §11.1 に一元化、(4)ドキュメント運用ルール(§0・変更履歴章)を新設 |
| 1.1 | 2026-06-12 | 直火方針を改訂。「縮小・終了方向／焚き火へ置換」を撤回し「提供継続・差別化軸不使用・SEO構造タグ禁止・本文残置」へ。CLAUDE.md §2.6 を「直火の扱い」に改題、spec §11.1 を同期。 |
| 1.2 | 2026-06-19 | §5 メタ情報の OGP/Twitter Card 定義を改訂。og:url（canonical一致）・og:type のページ種別切替を明文化、canonical の pathname 正規化を追記、twitter:title/description は付与不要(X の og: フォールバックにより冗長)と明記。 |
| 1.3 | 2026-06-19 | spec §11.2「売店の量り売り単位」を新設。生ビールは重量(g)量り売り（泡で容積が計れないため）で ml 表記に変更しない旨を予防注記として明記。サーバーのハイボール量り売り終了も記録。 |
| 1.4 | 2026-06-25 | §3/§11 を forest-daily の VPS公開API化（microCMS除去・クライアントfetch移行）に同期。実ソース `forestDaily.ts` を正として訂正（データ源=OirasApp read API、取得=client-fetch、videoUrl削除、フォールバックheroのみ）。 |
| 1.5 | 2026-06-26 | `spec-2026-05.md` を `spec.md` にリネーム。spec.md §13「/sites/（区画紹介）アーキテクチャ」を新設（3層モデル＝客層SEO入口/サイズ区分=予約単位/個別区画=動的、確定2テーブル）。keyword-map.md を /sites/ レイヤー新設に伴い改訂（柱5・別格・カニバリ項の落とし先を /sites/ へ移管）。 |
